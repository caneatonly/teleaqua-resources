# 仿真环境新电脑部署步骤

本文档用于在一台新电脑上复现当前 TeleaquaH4Z 和 TeleaquaH8仿真环境。目标效果：

- Docker image 提供 PX4、Gazebo Harmonic、ROS 2 Humble 运行依赖和已编译 PX4 环境。
- `external` 从 GitHub 拉取，并挂载到容器内 `/home/user/external`。
- `ros2_ws` 从 GitHub 拉取，并挂载到容器内 `/home/user/ros2_ws`。
- 容器内重新编译挂载进来的 Gazebo 插件和 ROS 2 workspace。

## 1. 宿主机安装基础工具

确认 Docker 可用：

```bash
docker version
docker ps
```

需要从私有 Docker Hub 仓库拉取镜像，先登录：

```bash
docker login
```

## 2. 拉取仿真资源仓库

以下命令在宿主机执行。

```bash
mkdir -p "$HOME/teleh4z-sim"
cd "$HOME/teleh4z-sim"

git clone --recurse-submodules https://github.com/TeleLoong/teleaqua-resources.git external
git clone https://github.com/TeleLoong/TeleaquaH4Z_ros_ws.git ros2_ws
```

确认 submodule 已拉取：

```bash
cd "$HOME/teleh4z-sim/external"
git submodule update --init --recursive
git submodule status --recursive
```

## 3. 拉取 Docker 镜像

以下命令在宿主机执行。

```bash
docker pull zyshine3/px4-h4z-dev:latest
```


## 4. 启动容器并挂载仓库

以下命令在宿主机执行。

```bash
xhost +local:docker

docker run -it --name teleh4z-sim \
  --network host \
  --privileged \
  -e DISPLAY="$DISPLAY" \
  -e NVIDIA_VISIBLE_DEVICES=all \
  -e NVIDIA_DRIVER_CAPABILITIES=all \
  -e QT_X11_NO_MITSHM=1 \
  -e PX4_ROOT=/home/user/PX4-Autopilot \
  -v /tmp/.X11-unix:/tmp/.X11-unix:ro \
  -v "$HOME/teleh4z-sim/external:/home/user/external" \
  -v "$HOME/teleh4z-sim/ros2_ws:/home/user/ros2_ws" \
  zyshine3/px4-h4z-dev:latest \
  bash
```

如果容器已经创建过，后续重新进入使用：

```bash
docker start -ai teleh4z-sim
```

需要打开第二、第三、第四个终端时，在宿主机执行：

```bash
docker exec -it teleh4z-sim bash
```

## 5. 容器内初始化挂载内容

以下命令在容器内执行。

```bash
cd /home/user/external
git submodule update --init --recursive

cd /home/user/external/plugins
./build_plugin.sh
```

将 `external` 中的 models、worlds、plugins、airframes 同步到 PX4 运行目录：

```bash
/home/user/sync_external_into_px4.sh
```

构建 ROS 2 workspace：

```bash
cd /home/user/ros2_ws
rm -rf build install log
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source install/setup.bash
```

## 6. 启动仿真

H4Z 和 H8 的启动流程相同：先启动 Micro XRCE-DDS Agent，再启动 Gazebo，最后启动对应机型的 PX4 SITL。

建议至少使用 3 个容器终端；如果需要 ROS 2 交互或模式管理，再打开第 4 个容器终端。

### 公共步骤 1：启动 Micro XRCE-DDS Agent

容器内执行：

```bash
MicroXRCEAgent udp4 -p 8888 > /dev/null 2>&1 &
```

### 公共步骤 2：启动 Gazebo

容器内执行：

```bash
SIM_WORLD=playground

python3 $PX4_ROOT/Tools/simulation/gz/simulation-gazebo \
  --model_store $PX4_ROOT/Tools/simulation/gz \
  --world ${SIM_WORLD} \
  --render_engine ogre2 &
```

### 方式 A：启动 TeleaquaH4Z

容器内另开终端执行：

```bash
cd $PX4_ROOT
PX4_GZ_STANDALONE=1 \
PX4_SYS_AUTOSTART=4026 \
PX4_SIM_MODEL=teleh4z \
PX4_GZ_MODEL_POSE="0,0,0.2,0,0,0" \
./build/px4_sitl_default/bin/px4 \
  -d "./build/px4_sitl_default/etc" \
  -s "etc/init.d-posix/rcS" \
  -i 0
```

H4Z 需要启动 ROS 2 模式管理与 Gazebo 桥接。容器内另开终端执行：

```bash
cd /home/user/ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash
ros2 launch teleh4z_manager mode_manager.launch.py
```

H4Z 模式切换测试：

```bash
cd /home/user/ros2_ws
source /opt/ros/humble/setup.bash
source install/setup.bash

ros2 topic pub --once /teleh4z/mode_request std_msgs/msg/String "{data: 'air'}"
ros2 topic pub --once /teleh4z/mode_request std_msgs/msg/String "{data: 'water'}"
```

### 方式 B：启动 TeleaquaH8

当前 H8 推荐使用带单目相机的模型：

```text
teleaquah8p_mono_cam
```

其中 `teleaquah8p_mono_cam` 会通过 `model://teleaquah8p` 引入 H8 本体模型。

容器内另开终端执行：

```bash
cd /home/user/PX4-Autopilot
PX4_GZ_STANDALONE=1 \
PX4_SYS_AUTOSTART=4024 \
PX4_SIM_MODEL=teleaquah8p_mono_cam \
PX4_GZ_MODEL_POSE="0,0,0.5,0,0,0" \
./build/px4_sitl_default/bin/px4 \
  -d "./build/px4_sitl_default/etc" \
  -s "etc/init.d-posix/rcS" \
  -i 0
```

参数说明：

- `PX4_SYS_AUTOSTART=4024`：加载 TeleaquaH8 airframe。
- `PX4_SIM_MODEL=teleaquah8p_mono_cam`：生成带单目相机的 H8P Gazebo 模型。
- `PX4_GZ_MODEL_POSE`：设置模型出生位置。
- `PX4_GZ_STANDALONE=1`：表示 Gazebo 已经单独启动，PX4 只连接并生成模型。

H8 启动后，可在容器内另开终端检查 ROS 2 话题：

```bash
source /opt/ros/humble/setup.bash
source /home/user/ros2_ws/install/setup.bash

ros2 topic list
ros2 topic echo /fmu/out/vehicle_status
ros2 topic echo /fmu/out/manual_control_setpoint
```

### 启动 QGroundControl

QGroundControl 建议在宿主机运行，不在容器内运行。因为容器使用 `--network host`，QGC 可以直接连接容器里的 PX4。

宿主机终端执行：

```bash
cd "$HOME/teleh4z-sim/external"
QT_QPA_PLATFORM=xcb ./QGroundControl.AppImage
```

QGC 可用于观察 MAVLink 状态和手柄控制。

## 7. 常用检查命令

以下命令在容器内另一个终端执行，用于确认 PX4 和 ROS 2 通信是否正常。

```bash
source /opt/ros/humble/setup.bash
source /home/user/ros2_ws/install/setup.bash

ros2 topic list
ros2 topic echo /fmu/out/vehicle_status
ros2 topic echo /fmu/out/manual_control_setpoint
ros2 topic echo /fmu/out/vehicle_air_water_status
```

## 8. 检查调用插件是否正确

在这个仿真项目中，有两个重要插件： 

`/home/user/external/plugins/hydrodynamics/build/libhydrodynamics.so`
`/home/user/external/plugins/bidir_motor_model/build/libgz-sim-bidir-motor-model-system.so`

Gazebo的SDF调用插件原理是从环境变量GZ_SIM_SYSTEM_PLUGIN_PATH的路径中搜索
因此,最好确保`/home/user/external/plugins/buoyancy/build:/home/user/external/plugins/hydrodynamics/build:/home/user/external/plugins/bidir_motor_model/build`在这个环境变量中,保证SDF调用的是我们自定义的插件

## 常见问题

### Gazebo GUI 打不开

宿主机执行：

```bash
xhost +local:docker
```

然后重新启动容器。

### Docker 容器名已存在

如果确认旧容器不需要保留：

```bash
docker rm teleh4z-sim
```

然后重新执行 `docker run`。

### ROS 2 找不到 teleh4z_manager

容器内执行：

```bash
cd /home/user/ros2_ws
source /opt/ros/humble/setup.bash
colcon build --symlink-install
source install/setup.bash
ros2 pkg list | grep teleh4z_manager
```

### PX4 找不到 4026 airframe

容器内执行：

```bash
ls /home/user/PX4-Autopilot/ROMFS/px4fmu_common/init.d-posix/airframes/4026_gz_teleh4z
ls /home/user/PX4-Autopilot/build/px4_sitl_default/etc/init.d-posix/airframes/4026_gz_teleh4z
```

如果文件不存在，重新同步：

```bash
/home/user/sync_external_into_px4.sh
```
